class CarInfoBox {
    drawBox(car){
        const boxHeight = height/2;
        const boxWidth = width;
        const boxX = 0; 
        const boxY = height/2;
        rect(boxX,boxY, boxWidth, boxHeight);
        
    }
}